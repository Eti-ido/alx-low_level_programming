#include "main.h"
#include <stdio.h>

/**
 * print_number -prints an integer
 * @n: The integer to be printed
 * Return: void
 */

void print_number(int n)
{
	if (n > 0)
	{
		_putchar(n);
	}

	else 
	{
		_putchar(n);
	}
}
